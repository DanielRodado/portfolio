---
interface Props {
    id?: string;
    className?: string
}

const {id, className} = Astro.props;
---

<section id={id} class=`w-full mx-auto px-4 md:px-0 ${className} md:max-w-2xl lg:max-w-4xl`>
    <slot />
</section>
